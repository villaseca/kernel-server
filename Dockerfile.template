FROM resin/%%RESIN_ARCH%%-node:slim

RUN npm install --global localtunnel

RUN JOBS=MAX npm install --production --unsafe-perm && npm cache clean && rm -rf /tmp/*

COPY start.sh /start.sh

COPY kernel-server.js /kernel-server.js

CMD ["sh", "/start.sh"]
